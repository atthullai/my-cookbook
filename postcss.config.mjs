// POSTCSS CONFIG MAP
// Tailwind CSS uses this during build/dev to turn app/globals.css into browser-ready CSS.

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
